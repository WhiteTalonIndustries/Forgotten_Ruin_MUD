# Backend Server Directory

This directory contains the main Django server configuration for the MUD.

- `settings.py`: Django settings with security, database, and channels configuration.
- `urls.py`: URL routing configuration.
- `asgi.py`: ASGI application for WebSocket support.
- `wsgi.py`: WSGI application for HTTP.
- `manage.py`: Django management script.
